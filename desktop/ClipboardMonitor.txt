if is pernamentlyBlock, can't raise event:
chová se jak má
stejná funkce jako monitor


afterSet - jak název napovídá, nastaví se před vložením do schránky. Vyhne se tak eventu o změně v schránce.


monitor - ClipboardHelper.SetText()
1. false/true
2. null, afterSet = true

set true/false funguje spolehlivě. načítal jsem z jiné app

teď přidat tlačítka 1-5 