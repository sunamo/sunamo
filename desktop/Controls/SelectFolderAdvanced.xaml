<UserControl x:Class="desktop.Controls.SelectFolderAdvanced"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:desktop.Controls"
             xmlns:desktop="clr-namespace:desktop"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <desktop:SelectFolder x:Name="selectFolder" FolderChanged="selectFolder_FolderChanged"></desktop:SelectFolder>
        <local:DialogButtons Grid.Row="1" x:Name="cDialogButtons" ChangeDialogResult="cDialogButtons_ChangeDialogResult">
            <local:DialogButtons.CustomControl>
                <CheckBox Content="Don't ask again"></CheckBox>
            </local:DialogButtons.CustomControl>
        </local:DialogButtons>
    </Grid>
</UserControl>
