<UserControl
    x:Class="apps.MenuItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:apps"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Button x:Name="btn" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" VerticalContentAlignment="Top" Background="Transparent">
        <Grid HorizontalAlignment="Left" VerticalAlignment="Top" x:Name="gridButtonContent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

                <Border BorderBrush="Yellow" BorderThickness="0" x:Name="txtIconBorder">
                    <!--Aby byl TextBlock zarovnaný na střed, musí být v nějakém obalu-->
                    <TextBlock x:Name="txtIcon" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBlock>
                </Border>

                <TextBlock x:Name="txtText" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left"></TextBlock>
        </Grid>
    </Button>
    
</UserControl>
