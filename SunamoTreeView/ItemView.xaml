<UserControl x:Class="SunamoTreeView.ItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Vertical">
        <MenuItem Command="{Binding Path=TickInThisItemAllCommand}" Header="Zaškrtnout všechny mimo podsložek"></MenuItem>
        <MenuItem Command="{Binding Path=TickInThisItemAndUnderAllCommand}" Header="Zaškrtnout všechny vč. podprvků"></MenuItem>
        <MenuItem Command="{Binding Path=UntickInThisItemAllCommand}" Header="Odškrtnout všechny mimo podsložek"></MenuItem>
        <MenuItem Command="{Binding Path=UntickInThisItemAndUnderAllCommand}" Header="Odškrtnout všechny vč. podprvků"></MenuItem>
    </StackPanel>
</UserControl>
